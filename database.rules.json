{
  "rules": {
    "drivers": {
      ".read": "auth != null",
      ".indexOn": ["geohash"],
      "$driverId": {
        ".write": "auth != null && auth.uid === $driverId"
      }
    },
    "trips": {
      ".read": "auth != null",
      ".indexOn": ["pickupGeohash"],
      "$tripId": {
        ".write": "auth != null && ((!data.exists() && newData.child('riderId').val() === auth.uid) || (data.exists() && data.child('riderId').val() === auth.uid) || (data.exists() && data.child('status').val() === 'requested' && newData.child('status').val() === 'accepted' && newData.child('driverId').val() === auth.uid) || (data.exists() && data.child('driverId').val() === auth.uid))"
      }
    }
  }
}