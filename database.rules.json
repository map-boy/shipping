{
  "rules": {
    ".read": false,
    ".write": false,

    "users": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid",
        "name": { ".validate": "newData.isString() && newData.val().length <= 100" },
        "phone": { ".validate": "newData.isString() && newData.val().length <= 20" },
        "email": { ".validate": "newData.isString() && newData.val().length <= 254" },
        "updatedAt": { ".validate": "newData.isNumber()" },
        "$other": { ".validate": false }
      }
    },

    "drivers": {
      ".read": "auth != null",
      ".indexOn": ["geohash"],
      "$driverId": {
        ".write": "auth != null && auth.uid === $driverId",
        ".validate": "newData.hasChildren(['lat', 'lng', 'vehicleType', 'status', 'geohash', 'lastUpdated'])",
        "lat": { ".validate": "newData.isNumber() && newData.val() >= -90 && newData.val() <= 90" },
        "lng": { ".validate": "newData.isNumber() && newData.val() >= -180 && newData.val() <= 180" },
        "vehicleType": { ".validate": "newData.val() === 'standard' || newData.val() === 'truck' || newData.val() === 'vip'" },
        "status": { ".validate": "newData.val() === 'online' || newData.val() === 'busy' || newData.val() === 'offline'" },
        "geohash": { ".validate": "newData.isString() && newData.val().length <= 12" },
        "lastUpdated": { ".validate": "newData.isNumber()" },
        "$other": { ".validate": false }
      }
    },

    "openTrips": {
      ".read": "auth != null",
      ".write": false,
      ".indexOn": ["pickupGeohash"]
    },

    "trips": {
      ".read": false,
      ".write": false,
      "$tripId": {
        ".read": "auth != null && (data.child('riderId').val() === auth.uid || data.child('driverId').val() === auth.uid)",
        ".write": false
      }
    },

    "activeTrips": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": false
      }
    },

    "tripHistory": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": false,
        ".indexOn": ["completedAt"]
      }
    },

    "bannedUsers": {
      ".read": false,
      ".write": false
    }
  }
}
